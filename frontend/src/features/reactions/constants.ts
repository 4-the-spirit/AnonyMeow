/**
 * Mirrors the backend's default Reactions:AllowedEmojis (appsettings.json). There's no
 * endpoint exposing the configured set, so this list must be kept in sync by hand.
 */
export const ALLOWED_REACTION_EMOJIS = ['👍', '👎', '❤️', '😂', '😮', '😢', '🔥']
