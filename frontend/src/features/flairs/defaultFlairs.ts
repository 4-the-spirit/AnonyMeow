// Mirrors backend/Common/DefaultFlairs.cs — every community launches with
// this fixed tag set, seeded server-side. Used here only to warn a creator before they try to
// add a custom tag that duplicates one of these names.
export const DEFAULT_FLAIR_NAMES = ['שאלה', 'דיון', 'עזרה', 'מחאה', 'הצהרה', 'מידע']
